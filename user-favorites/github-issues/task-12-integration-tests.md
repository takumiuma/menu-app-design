# Task 12: 統合テストの実装

## 概要

お気に入り機能の統合テストを実装し、全要件が正しく動作することを検証する。お気に入り追加/削除フロー、お気に入りページ表示、認証チェックのテストを含む。

## 要件

- 全要件の検証: 要件1〜4の受け入れ基準を満たすことを確認

## 実装内容

### 1. お気に入り追加/削除フローのテスト

```typescript
// tests/integration/favorite-flow.test.ts
describe('お気に入り機能フロー', () => {
  test('認証済みユーザーがお気に入りを追加できる', async () => {
    // 認証状態のセットアップ
    // メニューページにアクセス
    // お気に入りボタンをクリック
    // お気に入り状態の変更を確認
    // 成功メッセージの表示を確認
  })

  test('認証済みユーザーがお気に入りを削除できる', async () => {
    // お気に入り済み状態のセットアップ
    // お気に入りボタンをクリック
    // お気に入り状態の変更を確認
    // 削除完了メッセージの表示を確認
  })

  test('未認証ユーザーにはお気に入りボタンが表示されない', async () => {
    // 未認証状態のセットアップ
    // メニューページにアクセス
    // お気に入りボタンが表示されないことを確認
  })
})
```

### 2. お気に入りページ表示のテスト

```typescript
// tests/integration/favorites-page.test.ts
describe('お気に入りページ', () => {
  test('認証済みユーザーのお気に入りリストが表示される', async () => {
    // 認証状態とお気に入りデータのセットアップ
    // お気に入りページにアクセス
    // お気に入りリストの表示を確認
    // メニュー詳細情報の表示を確認
  })

  test('空のお気に入りリスト時に適切なメッセージが表示される', async () => {
    // 認証状態のセットアップ（お気に入りなし）
    // お気に入りページにアクセス
    // 「お気に入りがありません」メッセージの表示を確認
  })

  test('未認証ユーザーはログインページにリダイレクトされる', async () => {
    // 未認証状態のセットアップ
    // お気に入りページにアクセス
    // ログインページへのリダイレクトを確認
  })
})
```

### 3. 認証チェックのテスト

```typescript
// tests/integration/auth-check.test.ts
describe('認証チェック', () => {
  test('有効なトークンでAPIアクセスできる', async () => {
    // 有効なAuth0トークンのセットアップ
    // お気に入りAPI呼び出し
    // 正常なレスポンスを確認
  })

  test('無効なトークンで401エラーが返される', async () => {
    // 無効なトークンのセットアップ
    // お気に入りAPI呼び出し
    // 401エラーの確認
    // ログインページリダイレクトの確認
  })

  test('他ユーザーのお気に入りにアクセスできない', async () => {
    // 異なるユーザーのトークンでセットアップ
    // 他ユーザーのお気に入り削除を試行
    // 403エラーまたは404エラーの確認
  })
})
```

### 4. コンポーネント統合テスト

```typescript
// tests/integration/components.test.ts
describe('コンポーネント統合', () => {
  test('FavoriteButtonがストアと正しく連携する', async () => {
    // ストアの初期化
    // FavoriteButtonコンポーネントのマウント
    // ボタンクリック
    // ストア状態の変更を確認
  })

  test('ナビゲーションメニューが認証状態に応じて表示される', async () => {
    // 認証状態の切り替え
    // ナビゲーションメニューの表示確認
    // お気に入りリンクの表示/非表示確認
  })
})
```

### 5. エラーハンドリングテスト

```typescript
// tests/integration/error-handling.test.ts
describe('エラーハンドリング', () => {
  test('ネットワークエラー時に適切なメッセージが表示される', async () => {
    // ネットワークエラーのモック
    // お気に入り操作の実行
    // エラーメッセージの表示確認
  })

  test('重複追加時に適切なエラーメッセージが表示される', async () => {
    // 既にお気に入り済みの状態
    // 再度お気に入り追加を試行
    // 重複エラーメッセージの確認
  })
})
```

## テスト環境セットアップ

### モックの設定

- Auth0認証のモック
- APIレスポンスのモック
- データベース状態のモック

### テストデータ

- テスト用ユーザーデータ
- テスト用メニューデータ
- テスト用お気に入りデータ

## 受け入れ基準

- [ ] お気に入り追加フローのテストが実装されている
- [ ] お気に入り削除フローのテストが実装されている
- [ ] お気に入りページ表示のテストが実装されている
- [ ] 認証チェックのテストが実装されている
- [ ] 未認証ユーザーのアクセス制御テストが実装されている
- [ ] エラーハンドリングのテストが実装されている
- [ ] コンポーネント統合テストが実装されている
- [ ] 全てのテストが成功する
- [ ] テストカバレッジが適切に設定されている
- [ ] モックが適切に設定されている

## 技術スタック

- Vitest (ユニット・統合テスト)
- Vue Test Utils (Vueコンポーネントテスト)
- Playwright (E2Eテスト)
- MSW (APIモック)

## 関連ファイル

- 全ての実装済みコンポーネント・ページ・ストア
- テスト設定ファイル
- モック設定ファイル
